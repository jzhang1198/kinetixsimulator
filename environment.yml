name: kinetics-simulator
channels:
  - defaults
dependencies:
  - python=3.9.12
  - numpy
  - matplotlib
  - seaborn
  - pip
  - pip:
    - scipy==1.8.1
    - plotly==5.11.0
    - ipywidgets
    - pytest
prefix: /opt/anaconda3/envs/kinetics-simulator
